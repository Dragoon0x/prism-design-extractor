# Prism docs

Nextra-powered docs site lives here (Phase 11). For now this folder holds
working notes for contributors.

## Index (to be built in Phase 11)

- **Getting started** — install, configure BYOK, run first extraction
- **Self-hosting** — Docker compose, env vars, resources, upgrade path
- **Architecture** — canonical tree, extractor contracts, evidence trail
- **Output formats** — spec per format + sample outputs
- **API reference** — REST + SSE + tRPC schemas
- **Eval harness** — how accuracy is measured and enforced
- **Contributing** — see `../CONTRIBUTING.md`

The v1 implementation plan lives in the repo's planning files; this docs
tree is the long-lived user-facing surface.
