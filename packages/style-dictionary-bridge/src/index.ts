/**
 * @prism/style-dictionary-bridge — Phase 9 owner.
 *
 * Wraps Salesforce Style Dictionary with Prism-specific transforms/platforms so
 * `@prism/outputs` can emit CSS, SCSS, JS, Figma tokens, iOS, and Android
 * from one DTCG tree.
 */
export {};
