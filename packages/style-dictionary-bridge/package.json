{
  "name": "@prism/style-dictionary-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Prism — Style Dictionary wrapper with Prism-specific transforms and platforms.",
  "license": "MIT",
  "exports": { ".": { "types": "./src/index.ts", "default": "./src/index.ts" } },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf .turbo node_modules"
  },
  "dependencies": {
    "@prism/shared": "workspace:*",
    "style-dictionary": "^4.2.0"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
