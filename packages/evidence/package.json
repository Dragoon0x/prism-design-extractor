{
  "name": "@prism/evidence",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Prism — evidence capture and linkage: screenshot crops, DOM selectors, computed-style snapshots.",
  "license": "MIT",
  "exports": { ".": { "types": "./src/index.ts", "default": "./src/index.ts" } },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf .turbo node_modules"
  },
  "dependencies": {
    "@prism/shared": "workspace:*",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
