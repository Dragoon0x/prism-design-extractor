/**
 * @prism/evidence — evidence capture + storage helpers.
 *
 * Planned:
 *   - `crop.ts` — sharp-backed crop-to-bbox + S3 upload, returning the key
 *   - `link.ts` — attach an `EvidenceItem` to a token or component
 *   - `selector.ts` — stable CSS selector generation (wraps @medv/finder)
 *
 * Every token in a Prism extraction MUST end with at least one evidence item.
 * This package makes that cheap.
 */
export {};
