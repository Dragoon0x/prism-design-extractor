{
  "name": "@prism/queue",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Prism — QStash producers, Upstash Redis pub/sub for SSE, and signature verification for worker routes.",
  "license": "MIT",
  "exports": {
    ".": { "types": "./src/index.ts", "default": "./src/index.ts" }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf .turbo node_modules"
  },
  "dependencies": {
    "@prism/shared": "workspace:*",
    "@upstash/qstash": "^2.7.20",
    "@upstash/redis": "^1.34.3",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
